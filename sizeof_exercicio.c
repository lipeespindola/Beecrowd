#include <stdio.h>

struct ponto{
    int x, y;
};

int main(){

    int *ponteiro;

    ponteiro = (int *) malloc(5 * sizeof(int)); //malloc eh um endereco do inicio do espaco alocado na heap

    for(int i = 0; )
}